[project]
name = "tmsignatures"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "py-feat",
    "python-dotenv>=1.0.1",
]

[tool.uv.sources]
py-feat = { git = "https://github.com/cosanlab/feat.git" }
